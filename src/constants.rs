use crate::complex;
use crate::{Complex, QState, Qubit};
use std::f32::consts::{FRAC_1_SQRT_2, SQRT_2};

// Inverse Root 2
pub const IR2: f64 = 0.7071067811865475;

pub const C_ZERO: Complex = complex!(0.0, 0.0);
pub const C_ONE: Complex = complex!(1.0, 0.0);
